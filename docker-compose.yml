services:
  recommender:
    build: .
    container_name: python_recommender
    ports:
      - "50051:50051"
    environment:
      - PYTHONUNBUFFERED=1
      - GRPC_SERVER_PORT=50051
      - STORY_SERVICE_ADDR=host.containers.internal:50052
